<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="at.ac.tuwien.caa.docscan">

    <!--
    Settings for launcher icons in Image Asset Studio:
    Trim: No
    Shape: None
    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/docscan_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <!--android:label="@string/app_name"-->

        <!-- We need a file provider to enable a sharing of the log file (which is saved in internal memory) -->
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="at.ac.tuwien.caa.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:exported="false"
            android:name=".sync.SyncService">
            <intent-filter>
                <action android:name="com.firebase.jobdispatcher.ACTION_EXECUTE"/>
            </intent-filter>
        </service>

        <activity
            android:name=".ui.StartActivity"
            android:theme="@style/SplashTheme"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.AboutActivity"
            android:exported="false"
            android:parentActivityName=".ui.CameraActivity"
            />

        <activity
            android:name=".ui.LoginActivity"
            android:exported="false"
            android:parentActivityName=".ui.CameraActivity"
            />

        <activity
            android:name=".ui.document.SeriesGeneralActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan"
            android:parentActivityName=".ui.CameraActivity"
            />

        <activity
            android:name=".ui.CameraActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize"
            />

        <activity
            android:name=".ui.syncui.UploadingActivity"
            android:exported="false"
            android:parentActivityName=".ui.CameraActivity"
            />


        <!--launchMode = "singleInstance" avoids opening the intent twice -->
        <!--nohistory = "true" creates the activity instead of just resuming it -->
        <activity
            android:name=".ui.document.CreateSeriesActivity"
            android:exported="true"
            android:launchMode = "singleInstance"
            android:noHistory="true"
            android:configChanges="orientation|screenSize"
            />

        <activity
            android:name=".ui.settings.PreferenceActivity"
            android:exported="false"
            android:parentActivityName=".ui.CameraActivity"
            />

        <activity
            android:name=".ui.settings.DebugPreferenceActivity"
            android:exported="false"
            android:parentActivityName=".ui.CameraActivity"
            />

        <activity
            android:name=".ui.LogActivity"
            android:exported="false"
            android:parentActivityName=".ui.CameraActivity"
            />

        <activity
            android:name=".ui.AccountActivity"
            android:exported="false"
            android:parentActivityName=".ui.CameraActivity"
            />

        <activity
            android:name=".ui.DocumentActivity"
            android:exported="false"
            android:parentActivityName=".ui.document.SeriesGeneralActivity"
            />

        <activity
            android:name=".ui.DropboxActivity"
            android:exported="true"
            android:parentActivityName=".ui.CameraActivity"
            />

        <activity
            android:name=".ui.LogoutActivity"
            android:exported="false"
            android:parentActivityName=".ui.CameraActivity"
            />

        <!--Debugging activities: -->


        <activity
            android:name=".sync.SyncStartActivity"
            android:exported="true"
            android:parentActivityName=".ui.CameraActivity"
            />
        <activity
            android:name=".ui.syncui.SyncActivity"
            android:exported="true"
            android:parentActivityName=".ui.CameraActivity"
            />


        <activity
            android:name="com.dropbox.core.android.AuthActivity"
            android:configChanges="orientation|keyboard"
            android:launchMode="singleTask">
            <intent-filter>
                <!-- Change this to be db- followed by your app key -->
                <data android:scheme="db-p2393aiyn7jdr54" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <receiver android:name=".sync.BootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>


    </application>



    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-feature android:name="android.hardware.camera"/>
    <uses-feature android:name="android.hardware.camera.autofocus"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-feature android:name="android.hardware.location.gps"/>

</manifest>